<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".filter_beta">

    <Button
        android:id="@+id/btn_choose"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:background="@drawable/rounded_rectangle"
        android:text="Choose image"
        android:textColor="@color/white">

    </Button>

    <ImageView
        android:id="@+id/img_filter"
        android:layout_width="match_parent"
        android:layout_height="400dp"
        android:layout_below="@id/btn_choose">

    </ImageView>

    <EditText
        android:id="@+id/filter_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="Filter type(0-24)"
        android:layout_below="@id/img_filter">

    </EditText>

    <EditText
        android:id="@+id/intensity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="Intensity (0/1)"
        android:layout_toRightOf="@id/filter_type"
        android:layout_below="@id/img_filter">

    </EditText>

    <EditText
        android:id="@+id/compressrate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="Compress Rate(0/1)"
        android:layout_below="@id/intensity">

    </EditText>

    <Button
        android:id="@+id/apply_filter"
        android:layout_marginTop="30dp"
        android:layout_width="400dp"
        android:layout_height="wrap_content"
        android:background="@drawable/rounded_rectangle"
        android:textColor="@color/white"
        android:text="Apply Filter"
        android:layout_below="@id/compressrate">

    </Button>

</RelativeLayout>